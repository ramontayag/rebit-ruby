module Rebit
  class VendorUserRecipientCollection < VendorUserResourceCollection

    # def all
    #   VendorUserRecipient.all(default_prefix_options)
    # end

    def create(attributes={})
      # VendorUserRecipient.create(attributes.merge(default_prefix_options))
    end

    # def find(id)
    #   VendorUser.find(id)
    # end

  end
end
